const SectionLayout = ({children}) => {
  return (
    <section className="p-24 px-8 tablet:p-16 tablet:px-7 xs:p-12 xs:px-7 light-gray">
      {children}
    </section>
);
}

export default SectionLayout;