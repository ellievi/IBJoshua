import Card from '../../Card';
import SectionLayout from '../SectionLayout';
import SectionTitle from '../SectionTitle';

const SectionMinistries = () => {
  return (
    <SectionLayout>
      <SectionTitle
        title="Ministérios"
      />
      <Card />
    </SectionLayout>
    
);
}

export default SectionMinistries;